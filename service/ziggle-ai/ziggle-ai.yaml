apiVersion: apps/v1
kind: Deployment
metadata:
  name: blms-transceiver
  labels:
    app: blms-transceiver
spec:
  replicas: 3
  selector:
    matchLabels:
      app: blms-transceiver
  template:
    metadata:
      labels:
        app: blms-transceiver
    spec:
      containers:
        - name: blms-transceiver
          image: minjuncho/blms-transceiver:9
          envFrom:
            - secretRef:
                name: blms-gpt-api-key-yaml
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: blms-transceiver-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
  selector:
    app: blms-transceiver
---
# apiVersion: traefik.containo.us/v1alpha1
# kind: IngressRoute
# metadata:
#   name: blms-transceiver
#   namespace: blms
#   annotations:
#     kubernetes.io/ingress.class: traefik-external
# spec:
#   entryPoints:
#     - websecure
#   routes:
#     - match: Host(`www.blms.transceiver.api.local.example.com`)
#       kind: Rule
#       services:
#         - name: blms-transceiver-service
#           port: 80
#     - match: Host(`blms.transceiver.api.local.example.com`)
#       kind: Rule
#       services:
#         - name: blms-transceiver-service
#           port: 80
#       middlewares:
#         - name: blms-headers
# tls:
#   secretName: local-example-com-tls
